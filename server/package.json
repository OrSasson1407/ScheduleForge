{
  "name": "scheduleforge-collab-server",
  "private": true,
  "version": "1.0.0",
  "description": "ScheduleForge's server: real-time collaboration relay plus accounts and the published schedule.",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/*.test.js",
    "test:ci": "firebase emulators:exec --only firestore --project scheduleforge-ci \"npm test\""
  },
  "dependencies": {
    "@sentry/node": "^10.0.0",
    "firebase-admin": "^13.0.0",
    "redis": "^4.7.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "firebase-tools": "^15.0.0"
  }
}
